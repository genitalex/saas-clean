import WorkspacePlaceholder from '@/components/layout/workspace-placeholder';

export const metadata = { title: 'Dashboard: Hoy' };

export default function Page() {
  return <WorkspacePlaceholder section='today' />;
}
