import WorkspacePlaceholder from '@/components/layout/workspace-placeholder';

export const metadata = { title: 'Dashboard: Actividad' };

export default function Page() {
  return <WorkspacePlaceholder section='activity' />;
}
