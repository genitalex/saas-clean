import WorkspacePlaceholder from '@/components/layout/workspace-placeholder';

export const metadata = { title: 'Dashboard: Configuración' };

export default function Page() {
  return <WorkspacePlaceholder section='settings' />;
}
